import { createTheme } from "@mui/material";

const Green = createTheme({
    palette: {
        mode: "light",
        primary: {
            main: "#019031"
        },
        secondary: {
            main: "#EAF0F1"
        }
    },
});

export default Green;

